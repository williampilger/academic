# Testes

Esses requests foram feitos usando a [extenção do vscode](https://marketplace.visualstudio.com/items?itemName=humao.rest-client), dando o comando `Ctrl + Alt + R` para executar o request selecionado. O resultado do request é mostrado em uma aba lateral, onde é possível ver o status code, os headers e o body da resposta.

